import { useState } from 'react';
import { useRouter } from 'next/router';
import View from './view';
import { PageType } from '~/types/variables';

const Page: React.FC = ({

}) => {

  return <View
        />
}

export default Page;